Student Form
<%= form_tag students_path do %>
  <label>First Name</label>
  <%= text_field_tag :first_name, @student.first_name if @student %>
  <label>Last Name</label>
  <%= text_area_tag :last_name, @student.last_name if @student %>
  <%= submit_tag "Submit Student" %>
<%
